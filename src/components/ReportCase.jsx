import React from "react";

const ReportCase = (props) => {
  return (
    <a
      href="mailto:report@hipenticton.com"
      className="btn btn-primary btn-lg mt-2"
      role="button"
      aria-disabled="true"
    >
      Case Reporting
    </a>
  );
};

export default ReportCase;
